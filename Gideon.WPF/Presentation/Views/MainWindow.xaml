<Window x:Class="Gideon.WPF.Presentation.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Gideon - EVE Online AI Copilot" 
        Height="900" 
        Width="1400"
        MinHeight="700"
        MinWidth="1000"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <materialDesign:DialogHost>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Top Bar -->
            <materialDesign:ColorZone Grid.Row="0" 
                                      Mode="PrimaryMid" 
                                      Padding="16,8">
                <DockPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                        <materialDesign:PackIcon Kind="Satellite" 
                                                 Width="32" 
                                                 Height="32" 
                                                 VerticalAlignment="Center"
                                                 Margin="0,0,12,0"/>
                        <TextBlock Text="Gideon" 
                                   FontSize="24" 
                                   FontWeight="Medium" 
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="EVE Online AI Copilot" 
                                   FontSize="14" 
                                   Opacity="0.8" 
                                   VerticalAlignment="Center"
                                   Margin="12,0,0,0"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <TextBlock Text="{Binding ConnectionStatus}" 
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0"/>
                        <materialDesign:PackIcon Kind="Circle" 
                                                 Width="12" 
                                                 Height="12"
                                                 VerticalAlignment="Center">
                            <materialDesign:PackIcon.Style>
                                <Style TargetType="materialDesign:PackIcon">
                                    <Setter Property="Foreground" Value="Red"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                            <Setter Property="Foreground" Value="LimeGreen"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </materialDesign:PackIcon.Style>
                        </materialDesign:PackIcon>

                        <Button Style="{StaticResource MaterialDesignToolForegroundButton}"
                                Command="{Binding StartAuthenticationCommand}"
                                Margin="16,0,0,0"
                                ToolTip="Authenticate with EVE Online">
                            <materialDesign:PackIcon Kind="AccountKey"/>
                        </Button>
                    </StackPanel>
                </DockPanel>
            </materialDesign:ColorZone>

            <!-- Main Content Area -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Navigation Sidebar -->
                <materialDesign:ColorZone Grid.Column="0" 
                                          Mode="Light"
                                          Padding="0">
                    <StackPanel>
                        <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                Command="{Binding NavigateToShipFittingCommand}"
                                Margin="8"
                                HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Rocket" 
                                                         Width="20" 
                                                         Height="20"
                                                         VerticalAlignment="Center"
                                                         Margin="0,0,8,0"/>
                                <TextBlock Text="Ship Fitting" 
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                Command="{Binding NavigateToMarketAnalysisCommand}"
                                Margin="8"
                                HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="TrendingUp" 
                                                         Width="20" 
                                                         Height="20"
                                                         VerticalAlignment="Center"
                                                         Margin="0,0,8,0"/>
                                <TextBlock Text="Market Analysis" 
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                Command="{Binding NavigateToCharacterPlanningCommand}"
                                Margin="8"
                                HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Account" 
                                                         Width="20" 
                                                         Height="20"
                                                         VerticalAlignment="Center"
                                                         Margin="0,0,8,0"/>
                                <TextBlock Text="Character" 
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                Command="{Binding NavigateToSettingsCommand}"
                                Margin="8"
                                HorizontalAlignment="Stretch">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Settings" 
                                                         Width="20" 
                                                         Height="20"
                                                         VerticalAlignment="Center"
                                                         Margin="0,0,8,0"/>
                                <TextBlock Text="Settings" 
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </materialDesign:ColorZone>

                <!-- Content Area -->
                <Grid Grid.Column="1" Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Main View Content -->
                    <ContentPresenter Grid.Row="0" 
                                      Content="{Binding CurrentView}"/>

                    <!-- Default Welcome Content -->
                    <Grid Grid.Row="0" Visibility="{Binding CurrentView, Converter={StaticResource NullToVisibilityConverter}}">
                        <StackPanel VerticalAlignment="Center" 
                                    HorizontalAlignment="Center">
                            <materialDesign:PackIcon Kind="Satellite" 
                                                     Width="64" 
                                                     Height="64"
                                                     HorizontalAlignment="Center"
                                                     Opacity="0.5"
                                                     Margin="0,0,0,16"/>
                            <TextBlock Text="Welcome to Gideon" 
                                       FontSize="24" 
                                       FontWeight="Medium"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,8"/>
                            <TextBlock Text="Your EVE Online AI Copilot" 
                                       FontSize="16"
                                       Opacity="0.7"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,24"/>
                            
                            <Button Style="{StaticResource MaterialDesignRaisedAccentButton}"
                                    Command="{Binding StartAuthenticationCommand}"
                                    Padding="24,12">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="AccountKey" 
                                                             Width="20" 
                                                             Height="20"
                                                             VerticalAlignment="Center"
                                                             Margin="0,0,8,0"/>
                                    <TextBlock Text="Authenticate with EVE Online" 
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <!-- Status Messages -->
                    <StackPanel Grid.Row="1" 
                                Orientation="Vertical" 
                                Margin="0,8,0,0">
                        
                        <!-- Loading Indicator -->
                        <ProgressBar IsIndeterminate="True" 
                                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                                     Margin="0,0,0,8"/>

                        <!-- Status Message -->
                        <TextBlock Text="{Binding StatusMessage}" 
                                   Visibility="{Binding StatusMessage, Converter={StaticResource NullToVisibilityConverter}}"
                                   Opacity="0.7"
                                   Margin="0,0,0,4"/>

                        <!-- Error Message -->
                        <materialDesign:Card Background="{DynamicResource ValidationErrorBrush}"
                                             Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"
                                             Margin="0,4">
                            <StackPanel Orientation="Horizontal" 
                                        Margin="16,8">
                                <materialDesign:PackIcon Kind="AlertCircle" 
                                                         Width="20" 
                                                         Height="20"
                                                         VerticalAlignment="Center"
                                                         Margin="0,0,8,0"
                                                         Foreground="White"/>
                                <TextBlock Text="{Binding ErrorMessage}" 
                                           VerticalAlignment="Center"
                                           Foreground="White"
                                           TextWrapping="Wrap"/>
                                <Button Style="{StaticResource MaterialDesignToolForegroundButton}"
                                        Command="{Binding ClearErrorCommand}"
                                        Margin="8,0,0,0"
                                        VerticalAlignment="Center">
                                    <materialDesign:PackIcon Kind="Close" 
                                                             Width="16" 
                                                             Height="16"
                                                             Foreground="White"/>
                                </Button>
                            </StackPanel>
                        </materialDesign:Card>
                    </StackPanel>
                </Grid>
            </Grid>

            <!-- Bottom Status Bar -->
            <materialDesign:ColorZone Grid.Row="2" 
                                      Mode="PrimaryDark" 
                                      Padding="16,4">
                <DockPanel>
                    <TextBlock Text="{Binding CurrentCharacter.Name, StringFormat='Character: {0}'}" 
                               DockPanel.Dock="Left"
                               VerticalAlignment="Center"
                               Visibility="{Binding CurrentCharacter, Converter={StaticResource NullToVisibilityConverter}}"/>
                    
                    <TextBlock Text="Ready" 
                               DockPanel.Dock="Right"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Right"/>
                </DockPanel>
            </materialDesign:ColorZone>
        </Grid>
    </materialDesign:DialogHost>
</Window>